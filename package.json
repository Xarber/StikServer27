{
  "name": "stikserver",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "main": "desktop/main.mjs",
  "scripts": {
    "start": "node server.mjs",
    "desktop": "electron .",
    "dist": "electron-builder --config electron-builder.config.cjs --publish never",
    "check": "node --check server.mjs && node --check discovery.mjs && node --check native-manager.mjs && node --check battery-history.mjs && node --check public/app.js && node --check desktop/main.mjs && node --check desktop/preload.cjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ffmpeg-static": "5.3.0"
  },
  "devDependencies": {
    "electron": "44.4.5",
    "electron-builder": "26.15.3"
  }
}
